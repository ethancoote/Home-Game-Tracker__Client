---
import '../../styles/global.css';
const {href, text, id} = Astro.props;
---
{href ? 
    <a class="button" href={href}>{text}</a>
:
    <button class="button" id={id}>{text}</button>
}


<style>
    .button {
        display: flex;
        white-space: nowrap; 
        height: fit-content;
        width: fit-content;
        background-color: hsl(0, 56%, 42%);
        border-radius: var(--border-radius);
        border: 1px solid hsl(0, 56%, 42%);
        padding: 8px 16px;
        box-sizing: auto;
        text-decoration: none;
        color: var(--white-200);
        font-size: 1rem;
        font-weight: 700;
        transition: .25s;
        font-family: inherit;

        &:hover {
            cursor: pointer;
            background-color: hsl(0, 100%, 51%);
            color: var(--white-100);
            box-shadow: 0px 3px 12px hsla(0, 0%, 100%, 0.3);
        }
    }
</style>
